apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mypvc
spec:
  resources:
    requests:
      storage: 500Ki
  volumeMode: Filesystem
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
